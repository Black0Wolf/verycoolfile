# راهنمای سوپر ساده و قدم‌به‌قدم اجرای ربات فوروارد تلگرام (از صفر تا صد)

این راهنما برای کسانی نوشته شده که هیچ تجربه‌ای ندارند. فقط کافیه دقیقاً همین دستورات رو کپی و پیست کنید.

---

## مرحله ۱: اتصال به سرور (VPS) از طریق ترموکس (Termux)
۱. برنامه **Termux** را در گوشی خود باز کنید.
۲. دستور زیر را تایپ کنید و به جای `YOUR_VPS_IP`، آی‌پی عددی سرور مجازی خود را بنویسید (مثلاً `192.168.1.1`) و اینتر بزنید:
```bash
ssh root@YOUR_VPS_IP
```
۳. اگر پیامی درباره `Are you sure you want to continue connecting` دیدید، کلمه `yes` را تایپ کنید و اینتر بزنید.
۴. رمز عبور سرور (Password) را تایپ کنید. (توجه: وقتی رمز را تایپ می‌کنید، هیچ حرفی روی صفحه نمایش داده نمی‌شود. این طبیعی است. فقط تایپ کنید و اینتر بزنید).

---

## مرحله ۲: نصب پیش‌نیازهای لازم
دستورات زیر را یکی‌یکی کپی کنید، در ترموکس پیست کنید و اینتر بزنید. صبر کنید تا هر کدام تمام شود:
```bash
apt update && apt upgrade -y
apt install python python-pip nano screen -y
```

---

## مرحله ۳: ساخت پوشه و فایل‌های پروژه
۱. یک پوشه جدید بسازید و وارد آن شوید:
```bash
mkdir telegram-forwarder
cd telegram-forwarder
```

۲. ساخت فایل تنظیمات (`.env`):
دستور `nano .env` را بزنید. حالا مقادیر زیر را داخل آن کپی کنید و به جای مقادیر پیش‌فرض، اطلاعات خودتان را بگذارید:
```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
PHONE=+989123456789
SOURCE_CHANNEL_ID=-1001234567890
DEST_CHANNEL_ID=-1000987654321
SESSION_NAME=forwarder
LOG_LEVEL=INFO
DROP_AUTHOR=True
SILENT=True
HISTORICAL_FORWARD=True
BATCH_SIZE=10
BATCH_COOLDOWN=2.0
```
**نحوه ذخیره در nano:** 
- کلید `Ctrl` و `O` (حرف O انگلیسی) را همزمان فشار دهید.
- دکمه `Enter` (بازگشت) را بزنید تا ذخیره شود.
- کلید `Ctrl` و `X` را همزمان فشار دهید تا از برنامه خارج شوید.

۳. ساخت فایل نیازمندی‌ها (`requirements.txt`):
دستور `nano requirements.txt` را بزنید، دو خط زیر را داخلش بنویسید و مثل قبل ذخیره و خارج شوید (`Ctrl+O` -> `Enter` -> `Ctrl+X`):
```text
telethon
python-dotenv
```

۴. ساخت فایل اصلی برنامه (`main.py`):
دستور `nano main.py` را بزنید. کل کد پایتون ربات را که در اختیار دارید اینجا پیست کنید. سپس مثل قبل ذخیره و خارج شوید (`Ctrl+O` -> `Enter` -> `Ctrl+X`).

---

## مرحله ۴: نصب کتابخانه‌های پایتون
این دستور را بزنید تا ابزارهای لازم دانلود شوند:
```bash
pip install -r requirements.txt
```

---

## مرحله ۵: اجرای اولیه و ورود به اکانت تلگرام
برای بار اول، باید ربات را به صورت عادی اجرا کنید تا کد تأیید تلگرام را از شما بخواهد:
```bash
python main.py
```
- از شما شماره تلفن (`Phone number`) را می‌خواهد (همان شماره‌ای که در `.env` نوشتید). اینتر بزنید.
- کدی که تلگرام برایتان می‌فرستد را وارد کنید و اینتر بزنید.
- اگر رمز دو مرحله‌ای (2FA) دارید، آن را هم وارد کنید.
- وقتی پیام `Listening for new posts...` را دیدید، یعنی ربات درست کار می‌کند.
- حالا برای بستن این حالت اولیه، کلیدهای `Ctrl` و `C` را همزمان فشار دهید تا برنامه متوقف شود.

---

## مرحله ۶: اجرای ربات در پس‌زمینه (تا با بستن ترموکس خاموش نشود)
برای اینکه ربات همیشه روشن بماند، از ابزاری به نام `screen` استفاده می‌کنیم:

۱. یک صفحه جدید و جداگانه بسازید:
```bash
screen -S forwarder
```
۲. داخل این صفحه جدید، ربات را اجرا کنید:
```bash
python main.py
```
۳. حالا که ربات در حال کار است، می‌خواهیم از این صفحه خارج شویم بدون اینکه ربات خاموش شود. 
**نحوه خروج:** 
- اول کلید `Ctrl` و `A` را همزمان فشار دهید و رها کنید.
- بلافاصله کلید `D` (حرف D انگلیسی) را فشار دهید.
- پیامی می‌بینید که نوشته `[detached]`. تبریک می‌گویم! ربات در پس‌زمینه در حال کار است و شما می‌توانید ترموکس را ببندید.

---

## مرحله ۷: چگونه ربات را بررسی یا خاموش کنیم؟

### الف) بررسی وضعیت ربات یا دیدن لاگ‌ها:
اگر خواستید ببینید ربات چه می‌کند، دوباره به ترموکس بروید و بزنید:
```bash
screen -r forwarder
```
بعد از بررسی، دوباره با `Ctrl+A` و سپس `D` از آن خارج شوید (خاموش نمی‌شود).

### ب) خاموش کردن کامل و اصولی ربات:
۱. اول به صفحه ربات برگردید:
```bash
screen -r forwarder
```
۲. کلیدهای `Ctrl` و `C` را همزمان فشار دهید تا برنامه متوقف شود.
۳. حالا که برنامه متوقف شد، خود صفحه `screen` هم بسته می‌شود.

### ج) اگر یادتان رفت خاموش کنید و می‌خواهید به زور ببندیدش:
اگر ترموکس را بستید و یادتان رفت ربات را خاموش کنید، دفعه بعد که آمدید این دستور را بزنید تا کلاً نابودش کنید:
```bash
screen -X -S forwarder quit
```

---
**نکته مهم:** فایل `forwarder_state.json` به طور خودکار ساخته می‌شود. اگر سرور ریستارت شد یا برنامه کرش کرد، نگران نباشید. دفعه بعد که اجراش کنید، دقیقاً از همان پیامی که مانده بود ادامه می‌دهد و پیام تکراری نمی‌فرستد.
